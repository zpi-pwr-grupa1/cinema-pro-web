import React, { Component } from 'react';
import { Switch, Route } from 'react-router-dom';

import './styles.scss';

const Views = (props) => (
  <div>
    <h2>Main page</h2>
  </div>
);

export default Views;
