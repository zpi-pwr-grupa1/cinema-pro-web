import React from 'react';

import Layout from 'components/Layout';
import Header from 'components/Header';

const App = (props) => (
	<Layout 
    header={
      <Header />
    }
  />
);

export default App;
